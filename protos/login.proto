syntax = "proto3";

package login;

service Greeter {
  rpc Login (LoginRequest) returns (LoginResponse);
}

message LoginRequest {
  string user = 1;
  string password = 2;
}

message LoginResponse {
  string message = 1;
}
